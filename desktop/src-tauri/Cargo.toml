[package]
name = "ai-director-studio"
version = "0.1.0"
description = "AI Director Studio desktop client"
authors = ["AI Director Studio"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
