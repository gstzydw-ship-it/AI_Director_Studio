# AI Director Studio 核心记忆与项目交接文档

> **致接手的 AI 助手 / 开发者：**
> 这份文档包含了截至 2026-04-18 的核心系统状态与刚刚调整的规则心智。请在开始任何新任务前仔细阅读，以保持开发上下文的绝对连贯。

## 一、当前系统架构与运行状态
1. **工作流引擎**：采用 LangGraph 构建了包括 `scene_analyst` (场景分析)、`story_planner` (结构规划)、`shot_director` (镜头设计)、`prompt_compiler` (编译师) 和 `quality_inspector` (质检导演) 在内的多 Agent 协作网络。
2. **状态机与持久化**：
   - 后端使用 FastAPI 提供接口，通过全局变量 `task_state` 管理当前前端展现的运行状态。
   - LangGraph 底层使用了 `SqliteSaver` (`checkpoints.sqlite`) 来保存多片段生成的连续对话记忆（这也是为什么如果任务卡死，必须同时清理该文件和 `state.json` 才能彻底重启）。
3. **前端 UI 机制**：采用纯原生 JS 轮询 `/api/status` 获取最新状态。由于多片段连续生成需要，页面在 DOMContentLoaded 时会自动恢复上一次的剧本、参考图和状态记录。

## 二、今日已解决的重大 Bug 与机制重构

### 1. 前端 UI 刷新数据丢失 Bug（已修复）
- **现象**：之前用户在生成等待期间或换片段时按 F5 刷新，会导致已上传的剧本和参考图消失，UI 重置。
- **修复方案**：已将 `script`、`aspect_ratio` 以及所有处理后的 `reference_image_b64s`（缩略图）深度存入后端的 `task_state` 中。现在刷新网页时，前端会自动执行 `_restoreUserInputs()` 读取后端状态并无缝复原整个交互界面。

### 2. 多机位片段中“配角反应动作被吃掉”的问题（已修复，知识库重大更新）
- **现象**：在 15 秒单一片段内，如果是主角动作 + 员工群体的即时反应，AI 视频大模型（如即梦/Seedance）会因为收到类似“锁定主体为主角、不抢中心”的约束词，导致运镜和焦点彻底锁死在主角身上，完全吃掉了后面的“横移到员工”或“切镜到员工”的指令。
- **修复逻辑（核心心智）**：
  - **总导演的绝对权威**：系统绝不通过“强制把 5 秒钟切成两半（拆片）”来妥协 AI 模型的缺陷，依然保留在一个片段内完成。
  - **知识库 07 号文件（Seedance输出词典）已做最高优先级修改**：
    - **【规则 12 重写】**：明确规定【约束段】只负责“服装连续、道具接续、场景不跳轴”等逻辑纠偏。**绝对严禁**约束段越权包含“锁定主体为A”、“禁止B抢中心”、“保持镜头稳定”等任何关于镜头控制的指令。
    - 约束段不得写任何霸王条款，一切切镜、运镜、多角色的序列转移（例如“前半段拍商北琛，后半段硬切至员工反应”），必须 100% 听从 `shot_director` 和 `prompt_compiler` 在【时间轴】正文里的安排。

### 3. 2026-04-18：15 秒导演片段多分镜口径（已统一）
- **用户偏好已确认**：不要把 AI 单次生成片段限制为“单机位连续拍摄”。系统应以“一个 15 秒导演片段可以包含多个主分镜/子分镜”为准。
- **已修改知识库**：`00`、`21`、`22` 和 `rule_registry.yaml` 已统一为“单条 Seedance prompt 可以承载有动机的切至、反打、局部特写、受击反应等镜头切换”。
- **已修改运行 prompt**：`agents/director_graph.py` 中 `story_planner`、`shot_director`、`prompt_compiler` 的硬编码提示已同步改成 15 秒多分镜策略。
- **关键约束**：允许切镜，但每次切换必须来自动作、视线、台词落点、受击反应或空间调度；禁止无动机跳轴、跳空间、跳主体。

### 4. 2026-04-18：知识库索引污染修复（已完成）
- **问题**：`rule_registry.yaml` 是规则注册表，但曾被普通向量库当成 `Agent: all` 的知识块注入，容易让所有 Agent 混读注册表全文。
- **修复**：`rule_registry.yaml` 已设置 `runtime_retrieval: false`；`agents/knowledge_base.py` 的 `_runtime_retrieval_enabled()` 已支持识别 YAML 顶层 header。
- **验证**：已重建向量库，普通向量库从 417 chunks 降到 350 chunks，`rule_registry.yaml` 不再进入普通向量库；专用 `query_rule_registry()` 仍可命中规则卡。

### 5. 2026-04-18：6 秒短片段与两段均分问题（已加硬约束）
- **问题**：旧运行中 `story_planner` 把第一段拆成 F01-F06 六个小片段，F01 只覆盖“商北琛入场+扫视”，导致 `prompt_compiler` 输出 `~6秒`、`0-3/3-6` 两段均分，且景别都接近中景/中近景。
- **根因**：上游拆片允许“动作不足时用更短片段”，校验器只检查有没有 `duration_target`，不检查是否低于 10 秒；最终 QC 也不检查时间轴段数、均分切段和镜头变化数量。
- **修复**：
  - `story_planner` 现在要求常规片段目标 10-15 秒，禁止把普通建立段/入场段/扫视段拆成 5-6 秒小片段。
  - `shot_director` 现在要求每个 10-15 秒片段至少 3 类景别/机位变化，相邻主分镜不得连续使用近似“正面/略低/中景/轻推近”组合。
  - `prompt_compiler` 现在要求 10-15 秒片段至少 4 个不等长时间段，必须出现建立/跟随、反应、局部特写、过肩/反打、群像、环境切away等至少 3 类镜头变化。
  - `_compiler_guard_report()` 已能把旧的 `~6秒 + 0-3/3-6 + 景别单一` prompt 判为失败。

### 6. 2026-04-18：视频复盘，防止从“单一”滑到“碎切”（已调整）
- **复盘视频**：`jimeng-2026-04-18-6215-1...mp4`，时长约 14 秒。
- **用户反馈**：脚步/地面特写是正确切点；但时间轴内部切得太碎，成片散乱且不连续；严飞迎上那段出现空间逻辑错误。
- **画面判断**：5 秒左右严飞被生成成正面中心人物，像主动迎上挡住商北琛行进轴线；后续又切商北琛近景、手部局部、群体散开，镜头关系过碎。
- **修复口径**：
  - 15 秒片段不再追求 4-6 个碎时间段，改为 3-4 个连续镜头组。
  - 允许脚步特写作为正确插入点，但每段最多 1-2 个关键插入特写。
  - 严飞/接待者必须固定在侧前方让路位，或以轻虚侧前景/画外声进入；禁止“严飞迎上/快步迎上/走上前挡住/穿越中轴”。
  - `19_Gold_Standard_Prompt范例.md` 已修正严飞范例和约束模板，避免继续污染输出。
  - `_compiler_guard_report()` 已能把“内部切换次数过多”和“严飞迎上阻挡中轴”判为失败。

### 7. 2026-04-18：分享版 API 配置与会话隔离（已完成）
- **owner 免填配置**：当前完整 API 配置已备份到 `config/private/settings.local.yaml`，本机运行优先读取该私有配置。
- **公开配置脱敏**：`config/settings.yaml` 已清空所有 `api_key` 字段，作为可分享模板。
- **统一 Comfly 网关**：WebUI 不再接收或保存浏览器 API 配置；所有 Agent 与向量库固定走后端 `https://ai.comfly.chat/v1` 配置。
- **请求级隔离**：`agents/request_context.py` 只保留 session 与流式输出上下文；API key/base_url 不再允许由浏览器请求覆盖。
- **任务状态隔离**：LangGraph 状态按浏览器 `session_id` 存到 `output/sessions/<session_id>/`；一键清空/中断/续跑只影响当前会话。
- **分享注意**：`.gitignore` 已忽略 `config/private/` 与 `output/sessions/`。打包分享时不要包含 `config/private/`。

## 三、下一步开发计划（Next Steps）

1. **一键清空任务功能已完成**
   - Web UI 已加入“一键清空任务”按钮。
   - 后端 `/api/reset` 会清空运行状态、删除持久化任务状态，并用 generation token 防止旧后台线程把已清空任务写回来。

2. **视频实测与第 2 段继承测试**
   - 需要验证修复后的纯粹 Prompt （带有子分镜硬切）丢进视频模型后，能否成功实现同片段内的硬切反应。
   - 需要验证在第一段生成完成后，系统弹出的“上传上一段尾帧”功能能否顺利完成首尾拼接，并流转到第 2 段的生成。

---
*Generated automatically by AI Pair Programmer. Ready for handover.*
