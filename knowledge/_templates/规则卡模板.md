---
rule_id: NEW-RULE-000
title: 新规则标题
doc_type: rule_card
rule_type: continuity
status: draft
priority: P3
runtime_retrieval: false
owner_agent: shared
agent_scope:
  - shared
served_agents:
  - shared
pipeline_stage: planning
retrieval_key:
  - 关键词一
  - 关键词二
applies_when:
  - 适用条件
avoid_when:
  - 不适用条件
failure_mode:
  - 这条规则主要防止什么问题
output_contract:
  - Agent 输出里必须体现什么
example_good:
  - 推荐写法示例
example_bad:
  - 禁止写法示例
conflicts_with: []
supersedes: []
created: <% tp.date.now("YYYY-MM-DD") %>
updated: <% tp.date.now("YYYY-MM-DD") %>
---

# 新规则标题

## 规则

写清楚这条规则要求什么。

## 适用 Agent

- shared

## 适用条件

- 什么情况下必须使用这条规则。

## 禁止项

- 什么情况下不要使用这条规则。

## 输出要求

- Agent 最终输出里必须出现或避免什么。

## 正确示例

```text
放一个可以直接给 Agent 学习的好例子。
```

## 错误示例

```text
放一个容易导致失败的反例。
```
