# AI Director Studio 规则文档（正式版）

> **文档版本**: v1.0  
> **生成时间**: 2026-04-29  
> **适用范围**: AI_Director_Studio 项目开发与维护

---

## 目录

1. [项目迁移与环境配置规则](#1-项目迁移与环境配置规则)
2. [director_graph 拆分迁移规则](#2-director_graph-拆分迁移规则)
3. [代码实现与验证规则](#3-代码实现与验证规则)
4. [知识库管理规则](#4-知识库管理规则)

---

## 1. 项目迁移与环境配置规则

### 1.1 新电脑恢复步骤

```bash
# 1. 解压项目
7z x AI_Director_Studio_Pack_20260416.zip -o"D:\AI_Director_Studio_Full_Backup"

# 2. 安装依赖
cd D:\AI_Director_Studio_Full_Backup
pip install -r requirements.txt

# 3. 重建向量库
python main.py build-db

# 4. 验证运行
python main.py          # Web UI模式
# 或
python test_conflict_fix.py  # 命令行测试
```

### 1.2 配置检查项

| 配置项 | 文件路径 | 说明 |
|--------|----------|------|
| `llm.api_key` | `config/settings.yaml` | 主LLM接口（comfly） |
| `agent_models.prompt_compiler.api_key` | `config/settings.yaml` | Comfly接口（Claude Opus） |
| `vectordb.api_key` | `config/settings.yaml` | 文档嵌入模型接口 |

### 1.3 代理设置

`main.py` 第29-31行硬编码了本地代理：
```python
os.environ["HTTP_PROXY"] = "http://127.0.0.1:9674"
os.environ["HTTPS_PROXY"] = "http://127.0.0.1:9674"
```
**规则**：新电脑代理端口不同时，需修改此配置。

---

## 2. director_graph 拆分迁移规则

### 2.1 核心目标

将单文件 `agents/director_graph.py` 渐进拆分为 `agents/director_graph_package/` 结构，满足：
1. **零行为变更**
2. **逐函数迁移**
3. **每迁一个函数都做新旧实现行为一致性验证**
4. **在下游仍直接 import monolith 期间，不删除旧实现**

### 2.2 迁移策略（必须遵守）

| 规则编号 | 规则内容 |
|----------|----------|
| **规则1** | 迁移时优先"复制源码原样落地"，不要顺手优化逻辑 |
| **规则2** | 旧文件 `agents/director_graph.py` 仍被外部直接 import，不可删除旧函数、不可贸然改导出方式、不可提前清空 monolith |
| **规则3** | 每迁一段必须验证：写 `_tmp_verify_xxx.py`，对比旧实现与新实现，样例至少覆盖正常路径 + 异常路径 + 空输入路径 |
| **规则4** | 优先复用 package 内已有基础设施，不要把已经迁出的内容又从 monolith 重新 import 回来 |

### 2.3 依赖管理规则

#### 2.3.1 package 分层关系

```
agents/director_graph_package/
├── types.py          # DirectorState, LLMSettings, constants
├── llm.py            # call_llm, resolve_llm_settings
├── state_store.py    # load_state, save_state, clear_state
├── prompting.py      # 片段解析、结构抽取、别名抽取等 prompt/文本基础设施
├── helpers.py        # 校验/运行时辅助
├── graph_api.py      # create_director_graph
├── nodes.py          # 全部公开 nodes
└── runners.py        # 所有公开 runner
```

#### 2.3.2 引用优先级

| 类型 | 优先来源 |
|------|----------|
| 文本抽取类基础设施 | `prompting.py` |
| 轻量校验 helper | `helpers.py` |

### 2.4 当前未完成迁移的函数

截至 2026-04-29，剩余 5 个重型 wrapper：

1. `_compiler_guard_report`（重校验链）
2. `_run_story_planner_with_schema_repair`（重执行链）
3. `_run_shot_director_single_pass`（重执行链）
4. `_run_llm_quality_inspector`（重执行链）
5. `_run_shot_director_review_board`（重执行链）

**规则**：这些函数包含完整 LLM 调用链、schema repair 重试逻辑、多阶段执行链，需整体迁移而非零碎迁移。

---

## 3. 代码实现与验证规则

### 3.1 验证流程

```
读取完整函数 → 分析依赖树 → 整块迁移 → 写新旧实现对比验证脚本 → 验证通过
```

### 3.2 验证脚本规范

验证脚本命名格式：`_tmp_verify_xxx.py`

#### 3.2.1 验证内容要求

| 验证类型 | 要求 |
|----------|------|
| 编译验证 | `python -m py_compile agents/director_graph_package/helpers.py` |
| 导入验证 | `import agents.director_graph_package.helpers as h` |
| 行为一致性验证 | 对比 `agents.director_graph` 旧实现 vs `agents.director_graph_package` 新实现 |

#### 3.2.2 样例覆盖要求

每个验证脚本的样例必须覆盖：
- ✅ 正常路径
- ✅ 异常路径
- ✅ 空输入路径

### 3.3 假迁移检测规则

**警惕**：表面拆分成功，实际运行路径仍然绕回 monolith

**检查项**：
1. `helpers.py` / `nodes.py` 是否还在 `from agents.director_graph import ...`
2. package 内函数是否只是 wrapper，而不是真实实现
3. 验证脚本对比的是不是"package 函数 vs monolith 函数"

---

## 4. 知识库管理规则

### 4.1 冲突修复记录

已完成的知识库冲突修复：

| 冲突编号 | 修复内容 |
|----------|----------|
| 05 | 规则07：从"必须换段"改为 A/B/C 三级条件化决策树 |
| 19 | 新增范例F（双人焦点转移）+ 范例A措辞调整 |
| 14/18 | 新增动作精度 S/A/B 三档 + 情绪锚点精度对齐 |
| 17 | 质检规则碎片段/受击者检查同步 |
| 22 | 多机位反应镜头措辞条件化 |

### 4.2 待处理事项

- 质检规则中"子分镜递进连接"的检查标准可能过严
- Web UI 打包验证（dist目录需重新执行 `build.bat`）
- 端到端测试的全面回归

### 4.3 关键文件索引

| 文件/目录 | 说明 |
|----------|------|
| `PROJECT_MEMORY.md` | 项目记忆文档，包含完整冲突清单与修复记录 |
| `knowledge/` | 17个知识库规则文件 |
| `agents/director_graph.py` | LangGraph核心流水线 |
| `agents/knowledge_base.py` | 知识库检索逻辑 |
| `config/settings.yaml` | API Key与模型配置 |
| `ui/app.py` | Web UI 后端入口 |
| `main.py` | CLI入口 |

---

## 附录：新电脑续接顺序建议

### 方案 A：先收口 validator 小尾巴
1. 补齐依赖：`_MAIN_SHOT_BLOCK_RE`、`_shot_director_hand_detail_continuity_issue`、`_segment_block`
2. 重跑验证：`_tmp_verify_dialogue_coverage.py`、`_tmp_verify_script_fidelity.py`、`_tmp_verify_source_event_coverage.py`

### 方案 B：处理重执行链（按优先级）
1. `_run_story_planner_with_schema_repair`
2. `_run_llm_quality_inspector`
3. `_run_shot_director_review_board`
4. `_run_shot_director_single_pass`

---

**文档结束**

> 本规则文档整合自以下来源：
> - `TASK_MEMORY_20260429_DIRECTOR_GRAPH_SPLIT.md`
> - `HANDOFF_20260428_DIRECTOR_GRAPH_SPLIT.md`
> - `HANDOFF_README.md`
